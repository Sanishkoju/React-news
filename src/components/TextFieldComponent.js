import React from 'react';

export const TextFieldComponent = ({ type, label, id, value, onChange }) => (
    <>
    <input id={input.id} {...input} />
    <label htmlFor={input.id} >{input.label}</label>
    </>
);